'use strict';

const should = require('should');
const s3 = require('../s3');

describe('S3', () => {
  it('should upload a file to s3', () => {
    // ...
  });
  it('should upload a file to s3 using stream', (done) => {
    // ...
  });
})
